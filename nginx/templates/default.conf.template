server {
    charset utf-8;
    listen 80;
    server_name _;

    root /usr/share/nginx/html;

    # Public Access Only: List all files
    location / {
        index  index.html index.htm;
        autoindex on;
    }
}